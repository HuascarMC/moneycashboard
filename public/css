body {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  overflow: auto;
  background: rgb(112, 110, 94);
  margin: 0;
}
.sidebar {
  display: flex;
  flex-direction: column;
  justify-content: space-between;

  min-height: 100%;
  width: 150px;
  border-right: 1px solid #D7DBDD;
  background-color: rgb(94, 82, 94);
  padding: 10px;
  align-items: stretch;
}

.sidebar_item {
  cursor: pointer;
  padding: 5px 10px;
  color: #16A2D7;
  font-size: 12px;
}

.sidebar_item.is-side-bar-item-selected {
  text-decoration-style: none;
  background-color: #EEF3F5;
  border-radius: 4px;
}

.erb {
  float: right;
  display: flex;
  margin-top: 450px;
  margin-left: 130px;

}

.wrapper {
  /**
   * Lay out the children of this container with
   * flexbox, which is horizontal by default.
   */
  display: flex;

  /**
   * Allow the children to wrap to the next "row",
   * instead of trying to squeeze them all into
   * a single row.
   */
  flex-wrap: wrap;
  height: 100%;
  width: 100%;
  border: 1px solid #D7DBDD;
}

.logo {

}
